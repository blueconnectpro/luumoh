name: luumoh_core
description: Shared Luumoh domain models and Supabase services.
publish_to: none
version: 0.1.0
resolution: workspace

environment:
  sdk: ">=3.6.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  geolocator: ^14.0.2
  http: ^1.2.2
  supabase_flutter: ^2.9.0
  uuid: ^4.5.1

dev_dependencies:
  flutter_lints: ^6.0.0
