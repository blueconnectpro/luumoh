name: luumoh_workspace
publish_to: none

environment:
  sdk: ">=3.6.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

workspace:
  - packages/luumoh_core
  - apps/customer_app
  - apps/rider_app
  - apps/store_app
  - apps/admin_dashboard
