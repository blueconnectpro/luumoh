name: luumoh_store_app
description: Store app for managing products, orders, and inventory.
publish_to: none
version: 1.0.0+1
resolution: workspace

environment:
  sdk: ">=3.6.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  file_picker: ^12.0.0-beta.7
  luumoh_core:
    path: ../../packages/luumoh_core
  mapbox_maps_flutter: 2.27.1
  supabase_flutter: ^2.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
